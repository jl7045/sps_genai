[project]
name = "sps_genai"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "pillow",
    "numpy",
    "torch",
    "torchvision",
    "tqdm>=4.66.0",
    "python-multipart>=0.0.20",
]
